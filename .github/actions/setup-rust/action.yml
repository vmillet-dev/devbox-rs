name: Setup Rust
description: Tauri system dependencies, Rust toolchain and a cargo cache shared between jobs.

inputs:
  shared-key:
    description: Cargo cache key. Jobs compiling the same thing must share the same value.
    required: true
  save-if:
    description: "'true' for the one job allowed to write the cache; the others read it."
    required: false
    default: 'false'

runs:
  using: composite
  steps:
    # webkit2gtk-4.1 exists from Ubuntu 22.04 on; patchelf is required by the AppImage bundler.
    - name: Tauri system dependencies
      if: runner.os == 'Linux'
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends \
          build-essential curl file libayatana-appindicator3-dev libgtk-3-dev \
          librsvg2-dev libssl-dev libwebkit2gtk-4.1-dev libxdo-dev patchelf wget

    # Read from `rust-toolchain.toml` rather than written here: two places would end up
    # diverging, and drift is precisely what pinning avoids.
    - name: Toolchain version
      id: toolchain
      shell: bash
      run: |
        channel=$(sed -n 's/^channel[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' rust-toolchain.toml)
        echo "channel=$channel" >> "$GITHUB_OUTPUT"
        echo "Pinned toolchain: $channel"

    - uses: dtolnay/rust-toolchain@master
      with:
        toolchain: ${{ steps.toolchain.outputs.channel }}
        components: clippy, rustfmt

    # `workspaces` is mandatory: the Cargo project lives in src-tauri/, not at the root.
    # Without it the action finds nothing and the cache stays empty without saying so.
    - uses: Swatinem/rust-cache@v2
      with:
        workspaces: src-tauri
        shared-key: ${{ inputs.shared-key }}
        save-if: ${{ inputs.save-if }}

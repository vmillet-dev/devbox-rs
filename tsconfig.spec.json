/* To learn more about Typescript configuration file: https://www.typescriptlang.org/docs/handbook/tsconfig-json.html. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": []
  },
  // Toutes les sources, pas seulement les specs : le plugin Angular de Vite
  // compile les composants sous test à partir de ce programme.
  "include": ["src/**/*.ts"]
}
